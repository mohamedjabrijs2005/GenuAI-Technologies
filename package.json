{
  "name": "genuai-technologies-monorepo",
  "private": true,
  "version": "2.0.0",
  "workspaces": [
    "genuai-frontend",
    "genuai-backend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=genuai-frontend",
    "build": "npm run build --workspace=genuai-frontend",
    "lint": "npm run lint --workspace=genuai-frontend"
  }
}
